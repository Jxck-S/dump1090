Custom Build of dump1090 Skyview Map 
Current build looks like this ![Preview](/Preview.png)
You can apply these modifications by taking the  files in public_html folder using them to replace the files in /usr/share/dump1090-fa and then restart lighttpd with "systemctl restart lighttpd".
